# 이 레포에 대해

`index.html` 하나로 이루어진 정적 사이트. FICC S&T 실무자에게 "AI 에이전트를 어떻게 적용하는지" 방법론을 가르치는 학습문서다. Vercel에 빌드 없이 그대로 배포한다. `README.md`에 배포 방법이 있다.

## 실습/예시 콘텐츠 원칙

5단계 방법론 본문은 특정 업무에 묶이지 않도록 일반적으로 쓴다. 실습 자료(가상 예약 확인 메일, 지시서 등)는 FICC 업무를 사실적으로 재현하려 하지 않는다. 작성자가 FICC 실무를 정확히 알지 못해 디테일을 틀리게 지어낼 위험이 있고, 에이전트 개념을 이해시키는 목적은 어떤 업무를 예로 들든 동일하기 때문에, 여행 일정 정리처럼 누구나 아는 일상 사례를 쓴다. 새 실습 예시를 추가할 때도 이 원칙을 따른다. FICC 도메인 지식이 필요한 디테일을 지어내지 말고, 일상 사례로 만든다.

실습 자료는 별도 섹션이 아니라 해당 단계 안의 `<details class="practice">` 토글에 둔다(02단계: 완성된 지시서 예시, 03단계: 가상 노트 + 지시서 붙여넣기 + Before/After + 따라 하기 순서). 예전에는 페이지 맨 아래에 "예시로 살펴보기"라는 별도 섹션이 있었지만, 각 단계가 자기 완결적이지 않고 페이지 점프가 많다는 피드백에 따라 없앴다. 새 실습 자료도 관련 단계 안에 토글로 넣고, 다시 별도 섹션으로 빼지 않는다.

# 말투 교정 작업 지침

사용자가 "말투 교정해줘" / "AI 티 나는 거 고쳐줘" 라고 하면, `index.html`의 한국어 본문 전체를 아래 두 체크리스트로 다시 훑어서 고친다. 코드/구조는 건드리지 않고 텍스트만 손본다.

## 참고 저장소

- https://github.com/blader/humanizer — Wikipedia의 "AI 작성 징후" 35개 패턴 기반. 영어 중심이라 아래 "적용 체크리스트"로 한국어에 맞게 옮겨뒀다.
- https://github.com/snflkd/fluent-korean — Claude Code용 자연스러운 한국어 output-style. 조사/어미 생략, 사전에만 있는 어휘, 번역체 문장 구조를 지적한다.

이 두 저장소는 매번 새로 fetch하지 말고, 아래 적용 체크리스트를 기준으로 삼는다. 체크리스트 자체를 더 다듬어야 할 일이 생기면 두 저장소를 다시 참고해서 이 파일을 갱신한다.

## 적용 체크리스트

**문장 습관**
- 에엠대시(—) 금지. 마침표, 쉼표, "다만"류 접속어로 푼다.
- 같은 문장 종결 패턴("~것이다", "~감각이다", "~목표다")을 문서 전체에서 반복하지 않는다. 짧은 구간 안에 두 번 이상 보이면 하나는 다른 구조로 바꾼다.
- "이것이 핵심이다", "결국 중요한 건" 류로 앞 내용을 다시 요약하는 문장은 군더더기다. 지운다.
- "~할 수 있다", "~것으로 보인다" 같은 과도한 헤지(hedging)나, 반대로 "누구에게나", "항상", "그대로 적용된다" 같은 근거 없는 단정은 둘 다 피한다. 확인 가능한 만큼만 말한다.
- 강제로 3개씩 묶어 나열하지 않는다. 실제로 그 개수여야 할 때만(위임의 3단계처럼 진짜 구조가 3가지인 경우) 유지한다.
- 같은 대상을 문서 여기저기서 다른 이름으로 부르지 않는다. 예: 지시서/지시문처럼 같은 걸 가리키는 단어가 섹션마다 다르면 하나로 통일한다(현재 통일어는 "지시서").

**단어 선택**
- 영어 그대로 옮긴 듯한 한자어/외래어 조합은 일상 어휘로 바꾼다. 예: 검수→확인, 인수인계하다→맡기다, 키트→자료, 목업→가상, 1차 버전→초안, 체크포인트→확인 사항.
- "패러다임", "시너지", "테스트먼트"류의 거창한 단어 대신 구체적 사실을 쓴다.
- 같은 단어(감각, 가깝다 등)를 좁은 지면 안에서 반복해 쓰지 않는다. 사전에서 골라 쓴 티가 나는 저빈도 어휘도 피한다.
- 괄호로 영어 원어를 병기하는 습관(예: "이 랩(Lab)은")은 꼭 필요한 고유명사가 아니면 없앤다.

**예외 (건드리지 않는 것)**
- `<pre>` 안의 가상 예약 확인 메일·지시서 예시는 그 자체가 "실제 문서처럼 보여야 하는" 콘텐츠이므로, 스타일 교정 대상이 아니라 예시 데이터로 취급한다.
- "하니스"는 한글 음역이 아니라 항상 영문 "harness"로 쓴다.
- FICC 시장 용어나 시세 티커 같은 장식 요소는 넣지 않는다(위 실습/예시 콘텐츠 원칙 참고). 이미 없는 걸 다시 넣는 방향으로 "분위기"를 살리려 하지 않는다.

**끝나면**
수정 후 브라우저로 한 번 렌더링해서 레이아웃이 안 깨졌는지 확인하고, `git add/commit/push`까지 마친다. 커밋 메시지에 어떤 패턴을 고쳤는지 한두 줄로 남긴다.
